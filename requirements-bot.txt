# Минимальные зависимости только для бота-кассы 24/7 (легкий образ ~150MB, быстро деплоится)
# Для генерации сайта (Pillow, moviepy) — ставятся в GitHub Actions отдельно, в Docker бота не нужны

requests
python-telegram-bot==20.7
Pillow
